{
  "name": "@cg/hooks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "echo 'no build step — Bun runs TypeScript directly'"
  },
  "dependencies": {
    "@cg/governance-core": "workspace:*",
    "@cg/policy-schema": "workspace:*",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.2"
  }
}
